<% if target.errors.any? %>
    <div id="error_explanation" class="notice">
      <h2><%= pluralize(target.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% target.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>