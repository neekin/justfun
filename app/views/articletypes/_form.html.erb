<%= form_for(@articletype) do |f| %>
  <% if @articletype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@articletype.errors.count, "error") %> prohibited this articletype from being saved:</h2>

      <ul>
      <% @articletype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
